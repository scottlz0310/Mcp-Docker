extends: default

rules:
  line-length:
    max: 200  # GitHub Actionsの長いURLに対応
  document-start: disable  # ドキュメント開始マーカーを無効化
  comments-indentation: disable  # コメントのインデントを緩和
  truthy: disable  # on/off値の制限を緩和
